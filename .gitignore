node_modules
.DS_Store
VortexRadar*/
server_data/

# Secrets — never commit. Real Stripe/DB values live in .env (untracked).
.env
.env.*
!.env.example

# Large sample NEXRAD files (not used at runtime; app fetches live data).
# Anchored so bundled app/**/data/ modules are still tracked.
/data/

# Local Global PTT gateway settings (holds the ingest key).
tools/ptt.local.env
tools/ptt.key.txt
